


1； synDataMarketingDao 这个方法是写同步用的的，不是写一些根据什么值取什么数据

	a)你下面这些方法加进来，以后你加一个减一个，我们就要同步一次？
	b)synDataMarketingDao里面的方法都是同步中间库数据的，你可以自己看下以前写的，下面这些取值的可以写在你们自己定义的文件下里面
	   也可以写在共用的MarketTerminalConfigDAOJdbc里面
	c)还有IEIGWSForMarketingImpl和下面那些装接的自己创建文件夹自己写，不要修改其他省的，业务不一样，表结构不一样，集抄和负控集中器终端规约都不一样
	d)还有不要改我的那些process装接流程的类，这边青海要装1800多个低压集抄，请不要随便去冲掉别人的文件，现在都在跑生产，出责任了不好玩
	e)ieig那个接口的每个省份自己建立一个实现
